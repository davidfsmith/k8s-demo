apiVersion: v1
kind: Service
metadata:
  name: httpbun1
  namespace: demo
spec:
  selector:
    app: httpbun
    instance: httpbun1
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
